primeironumero = input('Digite um número: ')
segundonumero = input('Digite um número: ')
soma = int(primeironumero) + int(segundonumero)

print('A soma dos números que você digitou é: {}.'.format(soma))

# Na aula, o professor colocou "int" já na linha das variáveis, exemplo: primeironumero = int(input('Digite um número:'))
# Isso pode até ser melhor, pois a variável toda fica "int". Colocando "int" só na linha do print, a variável é considerada "int" só pra função print.
